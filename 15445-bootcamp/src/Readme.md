# Latches vs Locks

Basically, `latches` are `OS locks` that protect shared memory in concurrent programs, and `locks` in a dbms are a higher level concept that protects the integrity of the database that the dbms is managing.


